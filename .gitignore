.next/
node_modules/
.env*
# ...except the documented list of variable names, which carries no values and
# is the only record of what this app needs to run.
!.env.example
*.tsbuildinfo
next-env.d.ts
.DS_Store
.vercel

# Python bytecode from scripts/
__pycache__/

# Claude Code: machine-local permissions and scratch worktrees. launch.json is
# shared on purpose — it is how the dev server gets started.
.claude/settings.local.json
.claude/worktrees/
